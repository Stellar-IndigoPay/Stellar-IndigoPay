name: Mobile Preview Build

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  ci-pass:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - run: exit 0

  build:
    if: github.event_name != 'push'
    name: EAS Preview Build
    runs-on: ubuntu-latest
    env:
      EXPO_TOKEN_SET: ${{ secrets.EXPO_TOKEN != '' }}
    defaults:
      run:
        working-directory: mobile
    steps:
      - name: Start Timer
        run: echo "START_TIME=$(date +%s)" >> $GITHUB_ENV
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-cached
        with:
          node-version: "22"
          working-directory: mobile
      - uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      - run: eas build --platform all --profile preview --non-interactive
        if: env.EXPO_TOKEN_SET == 'true'
      - name: Budget Check
        if: always()
        uses: ./.github/actions/budget-check
        with:
          start-time: ${{ env.START_TIME }}
          max-seconds: '900'

  deploy-ota:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    name: EAS OTA Update (Staging)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-cached
        with:
          node-version: "22"
          working-directory: mobile
      - uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      - name: Staged Rollout (Staging)
        run: eas update --branch staging --message "$(git log -1 --oneline)" --non-interactive

  promote-ota:
    if: github.event_name == 'workflow_dispatch'
    name: Promote OTA to Production
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-cached
        with:
          node-version: "22"
          working-directory: mobile
      - uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      - name: Promote to Production
        run: eas update --branch production --message "Production Promotion" --non-interactive
